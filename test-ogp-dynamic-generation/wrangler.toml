name = "test-ogp-dynamic-generation"
main = "build/worker/shim.mjs"
compatibility_date = "2023-03-22"

[build]
command = "cargo install -q worker-build && worker-build --release"

[[r2_buckets]]
binding = 'OGP_BUCKET'
bucket_name = 'test-ogp-dynamic-generation'
preview_bucket_name = 'test-ogp-dynamic-generation'
